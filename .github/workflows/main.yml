name: Test

on: [push]

jobs:
  tests:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - uses: jiro4989/setup-nim-action@v1
      - run: nimble test
      - name: Test for ERR_EMPTY_RESPONSE bug
        working-directory: ./tests/err_empty_response/
        run: pwd
